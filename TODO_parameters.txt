# Parameter Governance TODOs

- replication.yaml: batch_size=256 — NOTE — document justification in thesis methodology section; \
  not a code task.
- replication.yaml: dropout_rate=0.2 — NOTE — document justification in thesis methodology section; \
  not a code task.
- sensitivity.yaml: spend_log_transform — DONE — addressable via: python tune.py --mode sensitivity \
  --config <hpo_winner> --param dataset.spend_scaler --values log robust
- sensitivity.yaml: regression_head_hidden — DONE — addressable via: python tune.py --mode sensitivity \
  --config <hpo_winner> --param model.regression_head_hidden --values 0 32 64
- sensitivity.yaml: kendall_warmup_epochs — DONE — addressable via: python tune.py --mode sensitivity \
  --config <hpo_winner> --param loss.warmup_epochs --values 0 5 10 20
- sensitivity.yaml: early_stopping_patience — DONE — addressable via: python tune.py --mode sensitivity \
  --config <hpo_winner> --param training.early_stopping_patience --values 5 10 15 20
- sensitivity.yaml: max_grad_norm — DONE — addressable via: python tune.py --mode sensitivity \
  --config <hpo_winner> --param training.max_grad_norm --values 0.5 1.0 2.0 0.0
- sensitivity.yaml: scheduled_sampling — DONE — addressable via: python tune.py --mode sensitivity \
  --config <hpo_winner> --param training.scheduled_sampling.max_prob --values 0.0 0.1 0.25 0.4
