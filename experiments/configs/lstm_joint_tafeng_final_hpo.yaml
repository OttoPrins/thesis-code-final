# Auto-generated by tune.py from experiments/configs_final/lstm_joint_tafeng_final.yaml
# HPO-selected config (sampler=grid). Promoted to primary thesis
# results by kaggle_runner.ipynb (3-seed final eval in Cell 10).
# HPO note: trials were scored on the holdout period; grid size=10 bounds
# data-driven overfitting risk. See tune.py docstring for full discussion.
# Best Optuna objective=0.5388 (trial #3, 10 trials, max_epochs=80)
dataset:
  name: tafeng
  raw_dir: data/raw
  calibration_weeks: 12
  holdout_weeks: 5
  loss_mask_mode: all
  min_active_weeks: 1
  val_fraction: 0.1
  split_seed: 42
  freq_cap: auto
  spend_scaler: robust
model:
  type: lstm
  max_week: 51
  hidden_size: 128
  dense_units: 128
  num_layers: 1
  dropout: 0.0
  dense_activation: linear
  keras_initialization: true
  joint: true
  spend_head: hurdle_lognormal
  state_feature_dim: 1
training:
  epochs: 150
  max_epochs: 150
  batch_size: 512
  val_batch_size: full
  drop_last_train: true
  lr: 0.001
  optimizer: adam
  eps: 1.0e-07
  weight_decay: 0.0
  max_grad_norm: 1.0
  amp_enabled: false
  early_stopping_patience: 30
  restore_best_checkpoint: true
  scheduler:
    type: none
  scheduled_sampling:
    enabled: false
  final_finetune:
    enabled: false
  seed: 42
loss:
  type: kendall
  n_tasks: 2
  warmup_epochs: 10
  spend_loss: nll
  spend_loss_normalize: true
  spend_loss_scale: 0.1
  freq_logvar_max: -1.0
  spend_logvar_max: 2.0
  spend_logvar_clamp:
  - -2.0
  - 2.0
output:
  run_name: lstm_joint_tafeng_final_hpo
  results_dir: results
  save_checkpoint: true
inference:
  n_scenarios: 30
  mode: sample
  temperature: 1.0
  decode_top_bin: false
calibration:
  temperature_scaling: false
  aggregate_scaling: false
  aggregate_shrinkage: 1.0
evaluation:
  weekly_discount_rate: 0.0
  fail_on_invalid: false
