torch>=2.0.0
pytorch-lightning>=2.0.0
numpy>=1.24.0,<2.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0
pyyaml>=6.0
tqdm>=4.65.0
wandb>=0.15.0
lifetimes>=0.11.3      # Pareto/NBD, BG/NBD, Gamma-Gamma benchmarks
openpyxl>=3.1.0        # UCI Online Retail II Excel workbook support
pytest>=7.0.0          # test suite (tests/test_gppm_recovery.py)
cmdstanpy>=1.2.0       # true Stan-backed GPPM; also requires installing CmdStan
# The old hierarchical Gamma-Poisson model is retained as a non-thesis diagnostic
# under the explicit benchmark name `gamma_poisson`; it is not reported as GPPM.
rpy2>=3.5.0,<3.6       # optional: Pareto/GGG via R BTYDplus; 3.6.x expects newer R C symbols on this setup
shap>=0.42.0           # Extension 3 covariate ablation
